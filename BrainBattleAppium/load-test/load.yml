config:
  target: 'http://localhost:4000'
  phases:
    - duration: 60
      arrivalRate: 100
      name: "Sustained load of 100 virtual users per second"

scenarios:
  - name: "Baseline API Load"
    flow:
      - get:
          url: "/api/health"
          expect:
            - statusCode: 200
